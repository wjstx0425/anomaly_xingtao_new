# BMW：工装 B 定位与底脚阈值敏感性（2026-09-12）

用户明确“我说的是B”：工装是亮色底脚下面的黑色区域，A不属于此次排除对象。保留A及亮色材料端面。参考mask在B黑色区域 [2450,2873,2630,2895] 内前景像素为0，因此无需再删材料，之前折弯修正后的参考仍适用。确认及mask SHA见 `artifacts/bmw_contour_fixture_B_20260912/fixture_B_confirmation.json`。全圈仍有49个参考法向不确定点，局部语义确认不代表整个参考通过。

## 正常001的六条候选

独立审查显示六条NG位于底脚下缘左、中、右三段，每段分别有精细和粗轮廓证据，不能当成六个独立实物缺陷。两种观测有重叠但不完全相同，含未知/支持间断，未为减少数量而强行融合。偏差约5.6–7.0 px；旧对齐图支持中右段可见边约5 px上移。用户已确认该图正常，因此这是开发规则与已知正常图的冲突。

## 比较层敏感性实验

复用9月12日八图实际观测，完全不重提边或改变图像；仅把参考坐标 [2400,2840,2650,2890] 内的底脚下缘设为单独弧段，比较4/6/8/10 px阈值。其他部位保持4 px，最小连续长度保持15 px，全部参考周长仍必检。未修改已使用的配置、核心算法或正式输出。

下表为全图NG候选数量，未知覆盖在各阈值下完全相同；所有正式结论仍REVIEW。

| 样本 | 局部4 px | 局部6 px | 局部8 px | 局部10 px |
|---|---:|---:|---:|---:|
| reference | 0 | 0 | 0 | 0 |
| normal_extra_001 | 6 | 1 | 0 | 0 |
| normal_extra_003 | 0 | 0 | 0 | 0 |
| defect_002 | 34 | 34 | 31 | 32 |
| defect_005 | 34 | 29 | 29 | 29 |
| defect_007 | 69 | 69 | 68 | 64 |
| defect_015 | 42 | 40 | 39 | 39 |
| defect_020 | 52 | 47 | 47 | 47 |

8 px下normal001无NG候选，但仍有51条REVIEW测量候选、约2338 px未知段；因此不是合格放行。8 px是本次观察到的局部开发候选，不是由足够样本校准的生产公差。只有两张用户确认的正常图（含参考），没有独立测试集，不能声称该值可泛化；更不能把全圈阈值统一放宽。

- defect_002，右边缘中下部，最下加强筋右端附近：局部4/8 px设置下，区域内NG候选数 5/5。
- defect_005，顶部小脚：局部4/8 px设置下，区域内NG候选数 3/3。
- defect_007，左侧外弧下半段：局部4/8 px设置下，区域内NG候选数 2/2。
- defect_007，右侧直边中部：局部4/8 px设置下，区域内NG候选数 7/7。
- defect_015，上侧斜边偏右：局部4/8 px设置下，区域内NG候选数 1/1。
- defect_015，左外弧上半段：局部4/8 px设置下，区域内NG候选数 2/2。
- defect_015，底部小脚：局部4/8 px设置下，区域内NG候选数 9/6。
- defect_020，顶部小脚：局部4/8 px设置下，区域内NG候选数 3/3。

以上只是已有目测区域与候选框的事后交叠核查，不是缺陷逐像素真值，也不能证明放宽后不会漏掉更小缺陷。局部放宽可能减少真实小缺陷敏感性，仍需更多确认合格件重复取放、最小目标缺陷和允许偏差来确定规则。

## 验证与复现

- 4 px重放与八图原始事件逐字段一致（仅弧段ID不同），不只是总数一致。
- 八图源SHA验证通过，所有阈值的有效覆盖保持不变，B mask检查通过。
- 本轮未改核心算法，未重复运行旧436项软件回归；运行了真实缓存重比、事件一致性校验、脚本编译与diff检查。

```bash
UV_CACHE_DIR=/tmp/bmw-uv-cache uv run --no-sync python artifacts/bmw_contour_fixture_B_20260912/sensitivity.py
UV_CACHE_DIR=/tmp/bmw-uv-cache uv run --no-sync python artifacts/bmw_contour_fixture_B_20260912/report.py
```

完整数值：`artifacts/bmw_contour_fixture_B_20260912/sensitivity.json`、`verification.json`及32份逐样本阈值结果。原图与原8图输出未改。本次结论是工装定位问题已解决，剩余问题集中于正常波动规则和参考校准/角点未知覆盖；并非检测已达到生产可用。
