请阅读《兴韬_BMW整圈外轮廓检测_Codex实现说明_V1.0.md》，
在当前已有仓库中增量实现单视角整圈可见外轮廓检测。

用户指定仓库为 wjstx0425/anomaly_xingtao_new，
分支为 feat/bmw-four-camera-runtime-cleanup。
先核对本地分支、提交和未提交修改；不要强制回退远端快照。
钢印判断已经完成，必须保留并优先复用其公共接口。
本轮不做左右件自动识别、冲压方向、孔槽或深度模型训练。

本轮先完成文档C0—C3：
1. 核对本地已有代码和两张2047×1545输入图；防止误用4024×3036的ROI。
2. 实现一次人工辅助的整圈标准建立、保存与加载。
3. 实现待测图整圈自动粗分割、局部精确提边和有效性检查。
4. 实现刚性定位、双向点到线段距离、连续异常和全周覆盖状态。
5. 输出整圈叠加图、未知边段、局部事件、JSON/CSV及软件测试。

旧左侧缺口脚本只作为回归来源，不是本轮终点。
不得限定只检查缺口附近；不得在待测图上人工描出缺口作为算法输出。
不得用参考轮廓补齐待测未知边缘，不得镜像/缩放/拉伸来消除缺陷。
尖角、上下小脚、暗边必须逐段有结果或明确UNKNOWN，不能静默删除。

先独立离线，不加载其他检测模型，不改相机采集或生产放行。
新增路径和CLI是待实现设计，不要把它们说成已有代码。
使用本机已有uv环境，保护原图、模型、钢印及未提交修改。
不自动提交或推送，不编造真实ROI、验收公差或测试通过记录。

请实际修改代码、运行能够运行的测试，并在完成后给出：
修改文件、实际运行命令、整圈参考和待测叠加图、缺口事件、
覆盖/未知段统计、测试结果、需要补充的真实数据。
如果暗边尚未可靠提取，明确指出具体边段与原因，不能报告整圈通过。
