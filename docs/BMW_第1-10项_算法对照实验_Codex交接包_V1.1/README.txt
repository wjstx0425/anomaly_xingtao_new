入口：先阅读V1.1主任务书与CODEX_START.txt。本包取代V1.0的实验调度。
唯一无监督深度基线为已有EfficientAD；不重试PatchCore、AnomalyDINO或其他AD。
第一批：冻结基线、R01整圈轮廓、R02a细线规则、D01 YOLO输入策略推理诊断。
后续：有框做监督检测输入对照；有mask做U-Net分割，其他监督架构按需比较。
configs均为草稿，尚未对应实际可运行软件；不要自动进入训练或覆盖现场配置。
reference中的轮廓文档只复用技术合同，任务范围和调度以本包V1.1为准。
templates中待运行不等于0分。没有新实验结果，也未重新核对远端代码。
本包不包含原始图片、客户需求原件、权重或已完成的检测程序。
