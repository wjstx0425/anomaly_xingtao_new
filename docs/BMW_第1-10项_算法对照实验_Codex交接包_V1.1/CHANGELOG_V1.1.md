# V1.1调整说明

## 用户决策
无监督深度分支仅使用现有EfficientAD。用户反馈PatchCore、AnomalyDINO此前已经试过、区别不大。本包据此停止重复试验，不把该反馈泛化为所有数据集的性能结论。

## 相比V1.0
1. 从实际候选队列、任务表、启动指令、配置、报告模板中移除L01/L02/L03，不新增其他无监督模型代替。
2. 冻结EfficientAD权重、预处理、mask、后处理和阈值。缺资产/来源不清时标明限制，不擅自重训。
3. 第一轮改为整圈轮廓、细线规则、YOLO26n输入策略诊断；E2不训练新网络。
4. 监督主线为有框后匹配输入训练、有mask后U-Net分割。SegFormer、YOLO实例分割及n/s容量比较均按具体问题启用。
5. 不能以EfficientAD的PASS/NG作为其他补检分支唯一运行门控。
6. 继续保留实体分组、锁定测试、逐类k/n、正确定位、误报/复检/错误、覆盖与完整运行代价的评价约束。
7. 已有轮廓、钢印、评测工具优先复用；不删除历史方法代码，不覆盖未提交修改，不自动提交或推送。

## 本次实际完成
修订文档和配置，并检查文档/队列一致性。没有训练模型、复跑样件或计算新增算法成绩；代码事实沿用旧任务书的核对快照。
