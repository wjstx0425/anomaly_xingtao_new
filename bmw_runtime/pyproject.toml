[project]
name = "bmw-inspection-runtime-env"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
dependencies = [
  "anomalib[bmw-lab,cu126]",
]

[tool.uv]
package = false

[tool.uv.sources]
anomalib = { path = "..", editable = true }
